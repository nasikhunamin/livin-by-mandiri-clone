import 'package:flutter/material.dart';

class InfoProductScreen extends StatelessWidget {
  const InfoProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Ini harusnya webview kali!")),
    );
  }
}
