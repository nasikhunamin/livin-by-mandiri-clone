name: register
description: Provide register features.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  ui:
    path: '../../libraries/ui'
  core:
    path: '../../libraries/core'
  localizations:
    path: '../../shared/localizations'
  dependencies:
    path: '../../shared/dependencies'
  gen:
    path: '../../shared/gen'
  config:
    path: '../../shared/config'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: